import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const AddFood = () => {
  return (
    <SafeAreaView>
      <Text>This is the add foods page</Text>
    </SafeAreaView>
  );
};

export default AddFood;
