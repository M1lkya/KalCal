inside convex/auth.config.ts you need to change the domain to the one provided to you by clerk inside the integrations tab for convex

you need to set the varibles inside example.env to the ones provided at clerks auth website